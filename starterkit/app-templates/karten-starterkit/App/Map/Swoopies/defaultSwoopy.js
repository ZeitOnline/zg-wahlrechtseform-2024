const defaultSwoopy = {
  from: [0, 0],
  to: [0, 0],
  label: '',
  anchor: 'top-left',
  textAlign: 'left',
  // color: '#222',
  factor: Math.PI / 4,
  offset: [0, 0],
  iconSize: [100, 20],
  // weight: 1.2,
  displayOutline: true,
  // outlineWeight: 3,
  // outlineColor: '#F5F5F5',
  minZoom: 3,
  maxZoom: 21,
};

export default defaultSwoopy;
